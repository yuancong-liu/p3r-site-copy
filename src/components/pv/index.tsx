import styles from './index.module.scss';

export const Pv = () => {
  return (
    <section className={styles['pv']}>
      <button className={styles['play']}>PLAY TRAILER</button>
    </section>
  );
}